"""
Top-level package for the Abel–Jacobi Networks code when using a ``src`` layout.

The actual public library remains the ``aj`` package (``src/aj``).
This file exists so that ``import src.util`` and similar imports work
when the repo root is on ``sys.path`` (as in the notebooks).
"""

